services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - blaeu_data:/data
    environment:
      - DATA_DIR=/data
      - BLAEU_ALLOW_REGISTRATION=${BLAEU_ALLOW_REGISTRATION:-false}
    restart: unless-stopped

volumes:
  blaeu_data:
    driver: local
